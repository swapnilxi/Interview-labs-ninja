# Root requirements.txt — used by Vercel Python runtime for api/index.py
# Sync this with backend/pyproject.toml when deps change.

# ASGI → Lambda adapter (required for Vercel Python serverless)
mangum>=0.19.0

# FastAPI stack
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
pydantic>=2.13.4

# Auth
passlib[bcrypt]>=1.7.4
pyjwt>=2.13.0
bcrypt<4.1
python-dotenv>=1.2.2

# PDF processing
pypdf>=6.14.2
pdfplumber>=0.11.0
python-multipart>=0.0.32
Pillow>=10.0.0

# Document processing
python-docx>=1.1.0

# External integrations
youtube-transcript-api>=1.0.3
google-auth>=2.30.0

# PDF generation
xhtml2pdf>=0.2.17
